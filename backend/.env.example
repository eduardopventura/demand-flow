# ===========================================
# CONFIGURAÇÕES DO SERVIDOR
# ===========================================
# NODE_ENV e PORT são definidos no docker-compose.yml
# Não é necessário definir aqui

# ===========================================
# JWT - AUTENTICAÇÃO
# ===========================================
# JWT_SECRET será gerado automaticamente na primeira execução
# Se quiser definir manualmente, descomente a linha abaixo e defina um valor seguro
# JWT_SECRET=your-secret-key-change-in-production

# ===========================================
# SMTP - EMAIL (Zoho Mail ou outro)
# ===========================================
# Configurações do servidor SMTP para envio de emails
SMTP_HOST=smtp.zoho.com
SMTP_PORT=465
SMTP_SECURE=true
SMTP_USER=seu-email@dominio.com.br
SMTP_PASS=sua-senha-de-app
SMTP_FROM_NAME=Gestor de Demandas
SMTP_FROM_EMAIL=seu-email@dominio.com.br

# ===========================================
# WHATSAPP - WEBHOOK N8N
# ===========================================
# URL do webhook do n8n que aciona a API de WhatsApp
# Para mais detalhes sobre o formato do payload, consulte docs/WHATSAPP_WEBHOOK.md
WHATSAPP_WEBHOOK_URL=https://seu-n8n.com/webhook/demandas
WHATSAPP_ENABLED=true

# ===========================================
# NOTAS IMPORTANTES
# ===========================================
# 1. Este arquivo é um template - copie para .env e configure com seus valores reais
# 2. JWT_SECRET será gerado automaticamente se não for definido
# 3. Nunca commite o arquivo .env com credenciais reais!
# 4. Para SMTP, use senha de aplicativo, não a senha da conta
# 5. O webhook do WhatsApp deve seguir o formato documentado em docs/WHATSAPP_WEBHOOK.md
