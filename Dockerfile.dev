# Development Dockerfile with hot reload
FROM node:20-alpine

WORKDIR /app

# Copy package files
COPY package*.json ./

# Install ALL dependencies (including dev)
RUN npm install

# Copy source (will be overridden by volumes in docker-compose.dev.yml)
COPY . .

# Expose Vite dev server port
EXPOSE 8080

# Start dev server with host binding for Docker
CMD ["npx", "vite", "--host", "0.0.0.0", "--port", "8080"]

